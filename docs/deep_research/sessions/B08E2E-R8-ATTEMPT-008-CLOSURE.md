# B08E2E-R8 — Attempt-008 Closure and Follow-up Pilot Designs

Status: `completed` / `real_single_paper_e2e_pass` within the frozen local single-paper scope. No new Provider call was made during closure.

Attempt-008 (`dr-run-8b30915e93a6e6b5ee8137c5`) completed the frozen path with one real GLM Structured Planner call, four local read-only Tool calls, one real GLM Single Writer call, deterministic EvidenceGraph/assessment/Report validation and a complete terminal. It used 4,215 input, 1,278 output and 5,493 total tokens at `3475.2` micros; replay matched the checkpoint and made zero external calls.

The five artifact files and SHA-256 values are recorded in [e2e_result_manifest.attempt-008.json](../e2e/v1.2/e2e_result_manifest.attempt-008.json). The artifact is immutable and was not regenerated, resumed or overwritten.

`author_review_required=true`, `publication_ready=false`, and semantic correctness is unverified. This is not a Web, multimodal, Multi-Agent, Critic or publication-quality result.

The closure also freezes, without execution, the two follow-up designs in [FOLLOWUP_PILOTS.md](../e2e/v1.2/FOLLOWUP_PILOTS.md): `cross_paper_comparison` with at least two independent Sources and source-specific Evidence/Citation grounding, and `insufficient_evidence` with no unsupported Claim, no fabricated Citation and a structured abstention reason. Both have new deterministic attempt/run IDs and absent artifact targets and require separate authorization.
